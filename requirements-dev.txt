# Dependências só para rodar a suíte de testes (tests/) — não vão para o Streamlit
# Community Cloud nem para produção. Inclui `-r requirements.txt` porque os testes
# importam o pacote `waterweave` de verdade (sklearn, shap, deltalake etc.), não usam mocks
# para as bibliotecas — só para I/O de arquivo (`read_table`, ver tests/conftest.py e
# tests/test_gold_features_ml_anual.py).
-r requirements.txt

pytest>=8.0

# ingestion/connectors/mapbiomas.py e connectors/sensoriamento_historico.py importam `ee` no
# nível do módulo — precisa estar instalado para os testes (test_mapbiomas.py,
# test_bronze_uso_solo.py, test_sensoriamento_historico.py) sequer COLETAREM, mesmo eles
# mockando toda chamada de rede/autenticação real (ver docstring desses arquivos). Não precisa
# de `earthengine authenticate` em CI — os testes nunca chamam `ee.Initialize` de verdade.
earthengine-api>=1.1

# ingestion/connectors/era5_cmip6.py importa `cdsapi`/`xarray` no nível do módulo — mesmo
# raciocínio acima, para test_era5_cmip6.py coletar e rodar em CI (que mocka toda chamada de
# rede/token real, ver docstring do módulo; não precisa de token CDS em CI). `netCDF4` é a
# engine que `xarray.open_dataset` usa por baixo para ler os arquivos baixados da CDS.
cdsapi>=0.7.7
xarray>=2024.1
netCDF4>=1.6
