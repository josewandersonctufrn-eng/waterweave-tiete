# Instala o pacote `waterweave` (src/) em modo editável — necessário para que
# `import waterweave...` funcione sem depender de configurar PYTHONPATH
# manualmente, tanto localmente quanto no Streamlit Community Cloud.
-e .

# Dados e Delta Lake (deltalake = bindings Python do delta-rs, sem JVM;
# troque por pyspark + delta-spark se migrar para processamento distribuído —
# mesmo formato de tabela, sem precisar reprocessar o histórico)
pandas>=2.2
deltalake>=1.0
openpyxl>=3.1
requests>=2.32  # conector ANA/SNIRH (connectors/ana_snirh.py)

# Modelagem híbrida
scikit-learn>=1.5
shap>=0.45  # models/ml/shap_analysis.py importa `shap` diretamente — faltava aqui (nenhum
            # ambiente que só rodasse `pip install -r requirements.txt` teria o módulo)
joblib>=1.4
mesa>=3.0
networkx>=3.0  # dependência real do mesa (mesa.discrete_space importa networkx sem condição);
                # deixado explícito porque a resolução de dependências do Streamlit Cloud não
                # o instalou automaticamente, mesmo sendo requisito de fato do mesa>=3.0

# Web / Dashboard
streamlit>=1.37
plotly>=5.22
folium>=0.17
streamlit-folium>=0.23
fpdf2>=2.8  # export em PDF do Relatório Automático (pure-Python, sem binário de sistema)
pyphen>=0.14  # hifenização automática do texto justificado dos PDFs (pt_BR/en/fr/es, pure-Python)

# As dependências abaixo são só para desenvolvimento LOCAL (orquestração e os conectores que
# usam Google Earth Engine ou a Copernicus Climate Data Store) — deliberadamente fora deste
# arquivo para manter o build do Streamlit Community Cloud rápido e enxuto. Instale à parte
# quando for trabalhar em `orchestration/`, `connectors/mapbiomas.py` (uso do solo) ou
# `connectors/sensoriamento_historico.py` (NDVI/temperatura de superfície histórico via
# Landsat — ver docstring do módulo para o passo `earthengine authenticate`, obrigatório e
# interativo, antes do primeiro uso), ou `connectors/era5_cmip6.py` (clima real ERA5/CMIP6 via
# CDS — ver docstring do módulo para o token pessoal `WATERWEAVE_CDS_API_KEY`):
#   apache-airflow>=2.9
#   earthengine-api>=1.1
#   cdsapi>=0.7.7
#   xarray>=2024.1  # abre os NetCDF baixados da CDS em connectors/era5_cmip6.py
#   netCDF4>=1.6    # engine de leitura NetCDF usada pelo xarray.open_dataset acima
